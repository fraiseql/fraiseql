{
  "name": "fraiseql-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.ts --bundle --outfile=dist/app.js --minify --target=es2020 --ignore-annotations"
  },
  "dependencies": {
    "luxen-ui": "^0.1.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0"
  }
}
