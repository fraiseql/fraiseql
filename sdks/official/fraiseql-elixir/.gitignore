# Elixir
/_build
/deps
*.beam
*.iml
.elixir_ls
.fetch
.mix/
.rebar3
erl_crash.dump
rebar3.crashdump
rebar.lock

# Mix
.mix/

# Dialyzer's PLT. `mix dialyzer` is one of this SDK's three CI gates and
# elixir-sdk.yml caches this exact path, so running the gate locally leaves 2.4 MB of
# build output untracked in the tree with nothing ignoring it.
/priv/plts/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
