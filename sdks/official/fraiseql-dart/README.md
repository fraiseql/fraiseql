# fraiseql-dart (official)

> ⚠️ **Status: Not Yet Implemented** — This SDK is a planned stub. No community Dart SDK exists yet.

The official v2 Dart/Flutter SDK for FraiseQL is a planned future addition.

## Contributing

If you would like to contribute a v2 Dart SDK, please open a discussion on the main repository.
The SDK must:

- Parse and validate `schema.compiled.json` produced by `fraiseql-cli compile`
- Emit the same schema JSON as the Python and TypeScript official SDKs (cross-SDK parity tests)
- Pass the golden fixture tests in `tests/fixtures/golden/`
