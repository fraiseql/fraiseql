[package]
name = "fraiseql-guest-transform"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.21"
serde_json = "1.0"

[profile.release]
opt-level = "z"
lto = true
strip = true
