# Cargo configuration for FraiseQL v2
# https://doc.rust-lang.org/cargo/reference/config.html

# Alias commands
[alias]
audit-fix = "audit fix"
b = "build"
bench-quick = "bench --no-fail-fast -- --quick"
bloat = "bloat --release --crates"
br = "build --release"
c = "check"
check-all = "check --all-targets --all-features"
clippy-all = "clippy --all-targets --all-features -- -D warnings"
cov = "llvm-cov --all-features --workspace --lcov --output-path lcov.info"
doc-open = "doc --all-features --no-deps --open"
expand = "expand"
f = "fmt"
nt = "nextest run --all-features"
outdated-all = "outdated --root-deps-only"
r = "run"
t = "test"
tr = "test --release"
tree = "tree --depth 3"
watch-check = "watch -x 'check --all-features'"
watch-test = "watch -x 'test --all-features'"

[build]
# Use all CPU cores for compilation
jobs = -1

[profile.dev.build-override]
# Optimize build scripts
opt-level = 2

# Profile optimizations
[profile.dev.package."*"]
# Optimize dependencies in dev builds for faster testing
opt-level = 2

[registries.crates-io]
protocol = "sparse"

# Enable mold linker for faster linking on Linux.
# Provides 3-5x speedup on full rebuilds and uses significantly less memory than lld,
# which matters on CI runners where OOM during linking can kill large test binaries.
# All CI jobs already install mold via apt; developers can install it with:
# sudo apt-get install mold   (Debian/Ubuntu)
# brew install mold           (macOS — no-op, mold is Linux-only)
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[term]
# Colored output
color = "always"
verbose = false
