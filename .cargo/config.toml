# Cargo configuration for FraiseQL v2
# https://doc.rust-lang.org/cargo/reference/config.html

# Alias commands
[alias]
audit-fix = "audit fix"
b = "build"
bench-quick = "bench --no-fail-fast -- --quick"
bloat = "bloat --release --crates"
br = "build --release"
c = "check"
check-all = "check --all-targets --all-features"
clippy-all = "clippy --all-targets --all-features -- -D warnings"
cov = "llvm-cov --all-features --workspace --lcov --output-path lcov.info"
doc-open = "doc --all-features --no-deps --open"
expand = "expand"
f = "fmt"
nt = "nextest run --all-features"
outdated-all = "outdated --root-deps-only"
r = "run"
t = "test"
tr = "test --release"
tree = "tree --depth 3"
watch-check = "watch -x 'check --all-features'"
watch-test = "watch -x 'test --all-features'"

[build]
# Use all CPU cores for compilation
jobs = -1

[profile.dev.build-override]
# Optimize build scripts
opt-level = 2

# Profile optimizations
[profile.dev.package."*"]
# Optimize dependencies in dev builds for faster testing
opt-level = 2

[registries.crates-io]
protocol = "sparse"

[term]
# Colored output
color = "always"
verbose = false

# To use mold linker locally for faster linking on Linux:
#   sudo apt-get install mold clang
#   RUSTFLAGS="-C linker=clang -C link-arg=-fuse-ld=mold" cargo build
# Not enabled globally here because not all CI jobs install mold.
