# FraiseQL Security Advisory Configuration
# See: https://docs.rs/cargo-audit/latest/cargo_audit/

[advisories]
ignore = [
  # Vulnerability: No upstream fix available
  "RUSTSEC-2023-0071",  # rsa: Marvin Attack timing sidechannel (transitive via sqlx-mysql, only with mysql feature)
  # Unsound: No alternative available
  "RUSTSEC-2026-0002"  # lru: IterMut violates Stacked Borrows (transitive via aws-sdk-s3, not on our code path)
]
informational_warnings = ["unmaintained"]
