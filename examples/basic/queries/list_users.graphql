# List all users
# Usage: curl -X POST http://localhost:8000/graphql -H "Content-Type: application/json" -d @queries/list_users.graphql

{
  "query": "query ListUsers($limit: Int) { users(limit: $limit) { id name email created_at } }",
  "variables": {
    "limit": 10
  }
}
