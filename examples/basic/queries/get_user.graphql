# Get a specific user by ID with their posts
# Usage: curl -X POST http://localhost:8000/graphql -H "Content-Type: application/json" -d @queries/get_user.graphql

{
  "query": "query GetUser($id: Int!) { user(id: $id) { id name email created_at } }",
  "variables": {
    "id": 1
  }
}
