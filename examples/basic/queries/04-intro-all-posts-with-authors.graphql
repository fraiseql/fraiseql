# Chapter 4: Denormalized Reads
# Fetch all posts with author identity. The v_post view joins tb_user at
# view-definition time, so no client-side join is needed.

query AllPostsWithAuthors {
  posts {
    id
    title
    content
    authorId
    authorName
    authorEmail
    createdAt
  }
}
