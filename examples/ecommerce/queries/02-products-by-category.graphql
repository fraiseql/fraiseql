# E-Commerce Example: Filter Products by Category
# Get all products in a specific category with pricing

query ProductsByCategory($category: String) {
  products(category: $category) {
    id
    name
    description
    price
    inventory
    category {
      id
      name
      description
    }
  }
}
