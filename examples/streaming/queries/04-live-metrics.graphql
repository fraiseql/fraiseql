# Streaming Example: Live System Metrics
# Monitor real-time system performance metrics

query LiveMetrics($metric: String, $limit: Int) {
  metrics(metric: $metric, limit: $limit) {
    id
    metric
    value
    timestamp
    source
  }
}
