# Streaming Example: User Activity Status
# Check presence and status of a specific user

query UserActivity($id: ID!) {
  userActivity(id: $id) {
    id
    username
    status
    lastSeen
    activeNow
  }
}
