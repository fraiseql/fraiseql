# Streaming Example: Live Messages
# Get recent messages with user activity status

query LiveMessages($limit: Int) {
  messages(limit: $limit) {
    id
    userId
    content
    timestamp
    reactions
  }
}
