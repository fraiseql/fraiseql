# FraiseQL Example - Dependencies
#
# Pinned to the FraiseQL v2 line. The fully-resolved, hash-free transitive set
# is in requirements.lock (generated with `uv pip compile requirements.txt`);
# install from the lock for reproducible builds.

# Core FraiseQL (authoring SDK). v2 has no `fastapi` extra — FraiseQL v2 is a
# compiled engine and Python is the authoring layer only.
fraiseql==2.8.0

# Database connectivity
psycopg[binary]>=3.1.0

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# Testing dependencies (dev)
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
