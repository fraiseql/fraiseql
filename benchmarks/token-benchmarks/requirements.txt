# LLM Token Test Suite Requirements

# Token counting
tiktoken>=0.5.2

# LLM providers (optional - choose based on your needs)
openai>=1.10.0
anthropic>=0.18.0
transformers>=4.36.0  # For local models
torch>=2.1.0  # For local models

# Async HTTP client for API calls
httpx>=0.25.0
aiohttp>=3.9.0

# Data processing and analysis
pandas>=2.1.0
numpy>=1.24.0

# Visualization (optional)
matplotlib>=3.7.0
seaborn>=0.12.0

# Code parsing and analysis
ast-grep-py>=0.12.0  # For AST-based code analysis
tree-sitter>=0.20.0  # For parsing multiple languages
tree-sitter-python>=0.20.0
tree-sitter-javascript>=0.20.0

# Testing and validation
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-benchmark>=4.0.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# JSON and YAML handling
pyyaml>=6.0.1

# Progress bars and CLI
rich>=13.7.0
click>=8.1.0
tqdm>=4.66.0

# Development tools
black>=23.0.0
ruff>=0.1.0
mypy>=1.7.0
types-pyyaml

# Database support (for validating generated code)
asyncpg>=0.29.0  # PostgreSQL
psycopg[binary]>=3.1.0

# GraphQL validation
graphql-core>=3.2.0
strawberry-graphql>=0.211.0  # For validation
