[pytest]
addopts = -x --tb=short
pythonpath = src
testpaths = tests
markers =
    unit
    integration
    e2e
    slow
    enterprise
    database
    forked
    regression: Tests that verify regression fixes
    rust: Tests that require the Rust extension
